<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Rails & Google Cloud Demo", root_path, class: "navbar-brand" %>

    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item">
        <% if user_signed_in? %><%= link_to "Sign Out #{current_user.email}", session_path, method: :delete, class: 'nav-link' %>
        <% else %><%= link_to "Sign In", new_session_path, class: 'nav-link' %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
